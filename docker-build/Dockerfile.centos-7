FROM centos:7

RUN yum install -y findutils rpm-build rpm-sign dos2unix expect

RUN mkdir -p /rpmbuild/{SPECS,SOURCES,BUILD,BUILDROOT,RPMS,SRPMS}

COPY ./docker-build/rpmbuild.sh /
COPY ./docker-build/rpmexpect /

RUN chmod +x /rpmbuild.sh && chmod +x /rpmexpect

ENTRYPOINT ["/rpmbuild.sh"]
